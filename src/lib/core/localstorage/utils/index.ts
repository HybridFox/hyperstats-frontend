import { parseJSON } from './json';
import { verifyStorageType } from './memory';
import {
  comparator,
  select,
  verifyPath,
  keyMatches,
  updateOrCreatePath,
} from './storage';

export const Utils = {
  parseJSON,
  verifyStorageType,
  comparator,
  select,
  verifyPath,
  keyMatches,
  updateOrCreatePath,
};
