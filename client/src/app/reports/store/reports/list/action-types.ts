import { MODULE, LIST } from '../../constants';

export const LIST_ACTIONS = {
    FETCH: `${MODULE}/${LIST}/FETCH`,
    CLEAR: `${MODULE}/${LIST}/CLEAR`,
    ADD_TO_LIST: `${MODULE}/${LIST}/ADD_TO_LIST`,
};
