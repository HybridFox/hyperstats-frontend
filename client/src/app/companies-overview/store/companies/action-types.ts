import { RECYCLERS_ACTIONS } from './recyclers/action-types';
import { ORGANISATIONS_ACTIONS } from './organisations/action-types';

export const ACTIONS = {
  RECYCLERS: RECYCLERS_ACTIONS,
  ORGANISATIONS: ORGANISATIONS_ACTIONS,
};
